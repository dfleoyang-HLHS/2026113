# 113班座位表

將 `index.html` 上傳至 GitHub repository，即可用 GitHub Pages 部署。

## GitHub Pages
1. 建立 repository。
2. 上傳 `index.html`。
3. Settings → Pages → Deploy from a branch。
4. 選擇 `main` 與 `/ (root)`，儲存後開啟網站。

座位異動會儲存在瀏覽器 localStorage；不同裝置或清除瀏覽資料後不會同步。
